// import { Pipe, PipeTransform } from '@angular/core';

// @Pipe({name: 'selectedFilters'})
// export class SelectedFiltersPipe implements PipeTransform {
//   transform(tipos) {
//     tipos.forEach(element => {

//     });
//     return tipos.filter(filtro => true);
//   }
// }
